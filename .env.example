# Example environment variables. Copy to `.env` for local development and adjust
# values. On hosting platforms (e.g. Render), define these variables in the
# service's dashboard instead of using a `.env` file.

# Database connection string (required)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME

# Optional: credentials for the bundled Postgres service
POSTGRES_USER=USER
POSTGRES_PASSWORD=PASSWORD
POSTGRES_DB=DBNAME

# Optional: Google Maps API key
GOOGLE_MAPS_API_KEY=YOUR_API_KEY_HERE
